# Example environment variables for local development
# Copy this to .env.local and set values appropriately

API_KEY=your_gemini_api_key_here
# Optional: explicitly specify a model or comma-separated fallbacks
# MODEL_NAME=gemini-3-flash-preview
# MODEL_FALLBACKS=gemini-3-flash-preview,gemini-1.5

# Note: Keep API keys server-side only; don't commit secrets to git
